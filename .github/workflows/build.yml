name: Build xinfc for AX3000T

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential git wget xz-utils

      - name: Download OpenWrt SDK (ARM64 ipq806x)
        run: |
          wget https://downloads.openwrt.org/releases/24.10.4/targets/ipq806x/generic/openwrt-sdk-24.10.4-ipq806x-generic_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst
          tar -xf openwrt-sdk-24.10.4-ipq806x-generic_gcc-13.3.0_musl_eabi.Linux-x86_64.tar.zst
          mv xinfc openwrt-sdk-24.10.4-ipq806x-generic/package/

      - name: Build xinfc
        run: |
          cd openwrt-sdk-24.10.4-ipq806x-generic
          make package/xinfc/compile V=s

      - name: List build outputs
        run: ls -R openwrt-sdk-24.10.4-ipq806x-generic/bin/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: xinfc-arm64
          path: openwrt-sdk-24.10.4-ipq806x-generic/bin/packages/
